#include <bits/stdc++.h>
using namespace std;
#define forn(i,n) for(int i=0; i<(n); ++i)
#define ll long long
#define pi pair<int,int> 
#define pq priority_queue
    // #define vii(a,n) vector<int> a(n); forn(i,n) cin>>a[i];
#define vii(a,n) vector<int> a(n); forn(i,n) cin>>a[i];
int help(){
  int a=0;
  int b=0;
  int c=0;
  int d=0;
  int e=0;
  int t=0;
  int u=0;
  int iii=0;
  vector<int>v4;
  for(int i=0;i<799;i++){
  }
}
int main()
{
  ll t = 1;
  cin>>t;
  while (t--)
  { 
    int n,m,k;
    help();
    help();
    help();
    help();
    help();
    help();
    cin>>n>>m>>k;
    vector<pair<int,int>>v;
    help();
    help();
    help();
    help();
    help();
    int x,y;
    help();
    help();
    help();
    help();
    help();
    cin>>x>>y;
    for(int i=0;i<k;i++){
        int s,d;
        cin>>s>>d;
    help();
    help();
    help();
    help();
    help();
        v.push_back({s,d});
    }
    help();
    help();
    help();
    help();
    int h=1;
    for(int i=0;i<k;i++){
        int a=abs(x-v[i].first);
    help();
    help();
        int b=abs(y-v[i].second);
    help();
    help();
    help();

        if((a+b)%2==0){
            // cout<<"YES"<<endl;
            h=0;
    help();
    help();
    help();
    help();
    help();
    help();
            break;
    help();
    help();
    help();
    help();
    help();
        }
    }
    help();
    help();
    if(h){
        cout<<"YES"<<endl;
    help();
    help();
    help();
    help();
    }
    if(!h){
        cout<<"NO"<<endl;
    help();
    help();
    help();
    help();
    }
    // if(k==1){
    //     cout<<"YES"<<endl;
    //     continue;
    // }
    // if(k>=3){
    //     cout<<"NO"<<endl;
    //     continue;
    // }
    // sort(v.begin(),v.end());
    // if(abs(x-v[0].first)+abs(v[0].second-y)==1 &&(abs(x-v[1].first)+abs(v[1].second-y)==1)){
    //     cout<<"YES"<<endl;
    // }
    // else{
    //     cout<<"NO"<<endl;
    // }



  }
  return 0;
}